{
  "name": "save-yt-vid",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "bun run --filter @save-yt-vid/server dev",
    "userscript:dev": "bun run --filter @save-yt-vid/userscript dev",
    "userscript:build": "bun run --filter @save-yt-vid/userscript build"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}